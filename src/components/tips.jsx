import React from 'react';

const tips = (props) => {

    return(
        <div>
            <div>
                <ul>
                    <li>Filter by a specific column by clicking on the table's column headers</li>
                    <li>Refine your search by selecting an option from the 'filter search by category menu'</li>
                    <li>Toggle between list and table views of the records with the 'toggle data view' button</li>
                </ul>
            </div>
        </div>
    )
}

export default tips;